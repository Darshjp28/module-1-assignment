# module-1-assignment
A multi-protocol industrial IoT data pipeline (MQTT, CoAP, AMQP) simulating real-time factory telemetry, topic-based broker routing, and wire-level packet analysis under simulated network loss.
